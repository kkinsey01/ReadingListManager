{
  "name": "readinglistmanager",
  "version": "1.0.0",
  "description": "Reading list manager web app",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && copyfiles -u 1 \"src/**/*.{html,css}\" dist/src && copyfiles -u 0 \"public/**/*\" dist/",
    "dev": "node dist/server.js",
    "build-run": "tsc && copyfiles -u 1 \"src/**/*.{html,css}\" dist/src && copyfiles -u 0 \"public/**/*\" dist/ && node dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "express": "^4.21.1",
    "mongoose": "^8.7.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.7",
    "copyfiles": "^2.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
